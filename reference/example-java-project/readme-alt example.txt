Compile the program by:
1) entering the JSmithLab1 folder via the command prompt
2) running the following:
	"javac src\lang\*.java"

Run the program by:
1) locating the path of you input file
2) entering the Lab0 folder via the command prompt
3) running the following:
	"java -cp src sProject inputfile outputfile"

